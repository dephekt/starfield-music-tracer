<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Local Mutagen (vendor) so we can pass ApplicableArchivePathsOverride for Linux string/BA2 resolution. -->
    <ProjectReference Include="../../vendor/Mutagen/Mutagen.Bethesda/Mutagen.Bethesda.csproj" />
  </ItemGroup>

</Project>
